import React from 'react';
import Layout from "@/components/Layout/Layout.jsx";
import Registered from "@/components/user/registered.jsx";

const RegisterPage = () => {
    return (
        <Layout>
           <Registered/>
        </Layout>
    );
};

export default RegisterPage;
